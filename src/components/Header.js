import React from 'react';

export const Header = () => {
    return (
        <header>
        <h2>Expense Tracker</h2>
        <p>This is a simple expense tracker application.</p>
        </header>
    );
}